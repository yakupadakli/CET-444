@model  ICollection<CollaborativeLearning.Entities.Group>

@{
    foreach (var item in Model)
    {
          <li>@Html.ActionLink(item.GroupName,"Show", "Groups", new {GroupId = item.Id }, new { tabindex = "-1" })</li>
    }
}